import { deepPurple, red, teal } from "@mui/material/colors";

const darkTheme = {
	palette: {
		// mode: "dark",
		primary: {
			main: deepPurple["A100"],
		},
		secondary: teal,
	},
};

export default darkTheme;
