import { Typography } from "@mui/material";
import { FC } from "react";
const ProfilePage: FC = () => {
	return (
		<>
			<Typography>Profiel page</Typography>
		</>
	);
};

export default ProfilePage;
